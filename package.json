{
  "name": "rates-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Monorepo for multiple applications",
  "scripts": {
    "dev": "pnpm --filter app dev",
    "build": "pnpm --filter app build",
    "preview": "pnpm --filter app preview",
    "lint": "pnpm --filter app lint"
  },
  "devDependencies": {},
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  },
  "packageManager": "pnpm@9.0.0"
}

