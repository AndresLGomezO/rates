# ============================================================================
# BOOTSTRAP LAYER - BACKEND CONFIGURATION (LOCAL - TEMPORARY)
# ============================================================================
# Temporary local backend for initial bootstrap
# Will be migrated to GCS after bucket is created and verified
# Configured by setup.sh on Mon Jan 26 06:12:12 -05 2026

terraform {
  backend "local" {
    path = "terraform.tfstate"
  }
}
