rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Default rule: deny all access
    // Override with specific rules for your storage paths
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}






